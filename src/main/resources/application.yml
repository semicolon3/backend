spring:
  application:
    name: legal-ai
  datasource:
    url: jdbc:postgresql://localhost:5432/legal_ai
    username: postgres
    password: min16489307!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: legalai-super-secret-key-for-jwt-signing-must-be-at-least-256-bits
  access-token-expiration: 3600000       # 1시간
  refresh-token-expiration: 604800000    # 7일
